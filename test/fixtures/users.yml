one:
  name: 'Test User 1'
  email: 'test1@example.com'
  # T-09 のテスト (ケース5, 6) で 'password' を使うための設定
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

two:
  name: 'Test User 2'
  email: 'test2@example.com'
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>